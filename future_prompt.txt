I have an existing Laravel application that currently follows the default Laravel structure where controllers, models, policies, migrations, and other classes are mixed inside global directories such as `app/Models`, `app/Http/Controllers`, etc.

I need to refactor this application into an **enterprise-grade modular architecture** inspired by **Domain-Driven Design (DDD)** while keeping the system simple and synchronous (no queues or background jobs).

The goal is to make the system **scalable, maintainable, and optimized for financial reporting**, while supporting complex Excel-based financial data imports.

The current functionality must be grouped into a module called **Jamgarma**, which represents a financial development fund. In the future, another module called **Gazna** will be added, so the architecture must ensure strong separation between modules.

The system must also process **complex financial Excel reports** containing multi-level column headers and hierarchical financial categories such as income, expenses, reserves, and balances. These reports contain district-level financial data and may evolve over time with new financial categories.

The architecture must therefore support **dynamic financial categories** and avoid rigid database schemas that directly mirror Excel columns.

Requirements:

1. Refactor the project into a **modular architecture** located inside:

app/
└── Modules/

2. Create a module called **Jamgarma** and move all existing functionality into it while preserving current behavior.

3. The Jamgarma module must follow an **enterprise clean architecture** structure:

app/
└── Modules/
└── Jamgarma/
├── Domain
│   ├── Models
│   ├── Policies
│   ├── Enums
│   └── ValueObjects
│
├── Application
│   ├── Services
│   ├── UseCases
│   ├── DTOs
│   └── Validators
│
├── Infrastructure
│   ├── Persistence
│   │   ├── Migrations
│   │   ├── Seeders
│   │   └── Repositories
│   │
│   ├── Excel
│   │   ├── Importers
│   │   └── Parsers
│   │
│   └── Providers
│
└── Interfaces
├── Http
│   ├── Controllers
│   ├── Requests
│   ├── Resources
│   └── Middleware
│
└── Routes

4. All namespaces must reflect the module structure, for example:

App\Modules\Jamgarma\Domain\Models
App\Modules\Jamgarma\Application\Services
App\Modules\Jamgarma\Infrastructure\Persistence\Repositories
App\Modules\Jamgarma\Interfaces\Http\Controllers

5. The system must support importing complex Excel financial reports with hierarchical headers. The Excel structure includes financial categories such as:

* Total income
* Total expenses
* Balance (deficit/profit)
* Land sales
* State property privatization
* Renovation projects
* Entrepreneurship support funds
* Mahalla development programs
* District improvement
* Social infrastructure
* Schools, healthcare, internal affairs, culture and sports
* Loan repayments
* New project financing
* Property damage compensation
* Returned funds
* Reserve funds
* Remaining balance

6. The database must follow a **normalized financial reporting schema**, not a column-based Excel mirror. Recommended core entities include:

* Reports (metadata for each financial report)
* Districts or projects
* FinancialCategories (hierarchical categories representing financial flows)
* FinancialRecords (fact table containing amounts per report, district, and category)

7. FinancialCategories must support a **tree structure** so hierarchical Excel headers can be mapped correctly.

8. Implement an **Excel import layer** that:

* reads multi-row headers
* builds a category mapping dynamically
* converts Excel rows into DTO objects
* stores data using application services

9. Ensure the following Laravel features remain fully functional after refactoring:

* route registration
* migration execution
* policy binding
* dependency injection
* service container bindings
* validation and request handling

10. The architecture must be designed so that a **future module called Gazna** can be added under `app/Modules/Gazna` using the same structure without interfering with Jamgarma.

11. The final architecture must prioritize:

* modular separation
* maintainable domain logic
* high-performance database queries
* flexibility for evolving financial report structures
* efficient handling of large Excel files without background processing

Do not remove existing functionality. Only reorganize and refactor the project while improving structure, scalability, and long-term maintainability.


______________________

Act as a Principal Software Engineer and Laravel Performance Architect with 30+ years of experience designing high-performance systems, enterprise backend architectures, and large-scale data processing platforms.

You are an expert in:

Laravel internals

PHP performance optimization

database optimization

memory management

high-traffic production systems

scalable backend architecture

user-experience aware backend design

You think like a systems engineer, not just a coder. You carefully design solutions that are fast, safe, memory-efficient, scalable, and production-ready.

Your task is to design and implement a Laravel-based large dataset processing system that builds cache while displaying real-time progress feedback to the user.

Important Constraints:

Do NOT use Redis

Do NOT use Queue Jobs

Do NOT use background workers

Use only Laravel + PHP + file cache

The system must still behave as if it were asynchronous from the user's perspective.

Core Goals:

Process extremely large datasets (100k–1M+ rows)

Prevent memory exhaustion

Maintain fast user experience

Show real-time progress updates

System Requirements:

Implement efficient dataset streaming using Laravel techniques such as:

chunk()

cursor()

lazy()

Track progress safely:

store percentage progress (0–100) in Laravel cache

make progress retrievable via an API endpoint

Frontend behavior:

page loads instantly

user clicks Start Processing

progress bar updates every second using polling

show percentage text above progress bar

once 100% reached, show completion message and results

Implement safe concurrency control:

prevent multiple processes from running simultaneously

use cache locking mechanism

Optimize for production scale:

select only required columns

eager load relationships to prevent N+1 queries

disable query logging during heavy processing

process data in chunk sizes between 100–500 rows

avoid large in-memory arrays

minimize disk I/O from file cache

reduce database query overhead

Structure the architecture professionally:

Controller for HTTP interaction

Service class for processing logic

Clear API routes

Separation of concerns

Improve perceived performance:

the UI must never freeze

the user should always see visual feedback

Design the frontend UI elements:

Start Processing button

Progress bar

Percentage text

Result display area above progress bar

Provide production optimization recommendations, including:

PHP configuration improvements

database indexing strategies

Laravel production commands

memory safety techniques

Think like a performance engineer and suggest additional improvements that typical developers might overlook.

Before writing code:
First briefly explain the architecture and reasoning behind your design choices.

Then provide:

Laravel routes

Controller

Service class

Frontend example (HTML + JS polling)

Performance optimization notes

Write the solution as if it will be deployed in a real enterprise production environment handling very large data loads.

Focus on:

speed

stability

maintainability

memory efficiency

clean architecture. 